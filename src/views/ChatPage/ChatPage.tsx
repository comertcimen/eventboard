import { Center } from "@mantine/core";

export const Chat = () => {
  return (
    <Center
      sx={{
        flexDirection: "column",
        gap: 10,
      }}
    >
      <div>Chat Page</div>
    </Center>
  );
};
