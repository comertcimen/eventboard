import { Center } from "@mantine/core";

export const PastEvents = () => {
  return (
    <Center
      sx={{
        flexDirection: "column",
        gap: 10,
      }}
    >
      <div>Past Events Page</div>
    </Center>
  );
};
