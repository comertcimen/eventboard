import { Center } from "@mantine/core";

export const Saved = () => {
  return (
    <Center
      sx={{
        flexDirection: "column",
        gap: 10,
      }}
    >
      <div>Saved Page</div>
    </Center>
  );
};
