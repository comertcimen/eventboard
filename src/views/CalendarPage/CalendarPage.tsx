import { Center } from "@mantine/core";

export const Calendar = () => {
  return (
    <Center
      sx={{
        flexDirection: "column",
        gap: 10,
      }}
    >
      <div>Calendar Page</div>
    </Center>
  );
};
